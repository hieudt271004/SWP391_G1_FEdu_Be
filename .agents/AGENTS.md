# FEdu Workspace Rules

- **Docker Syncing**: Whenever you make code modifications (frontend or backend), always automatically update and sync the running Docker environment (e.g. by running `docker compose restart frontend` or rebuild/restart command) upon finishing the changes, without waiting for the user to ask.
